import { Meta, Story } from '@storybook/addon-docs';
import * as ClassNameStories from './ClassName.stories.mjs';

<Meta of={ClassNameStories} />

# className

If specified, a `className` prop may be directly given to each component.

Use the developer tools to inspect the classes of the rendered example's DOM elements.

### Example:

<Story of={ClassNameStories.Example} />

### Implementation:

<Story of={ClassNameStories.Implementation} />
